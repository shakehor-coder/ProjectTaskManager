<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ProjectManager.ViewModels"
             x:Class="ProjectManager.Views.MainPage"
             Title="Управление задачами проекта">

    <VerticalStackLayout Padding="20" Spacing="10">
        <Entry x:Name="TaskEntry"
               Placeholder="Введите название задачи"
               Text="{Binding NewTaskTitle}" />
        
        <Button Text="Добавить задачу"
                Command="{Binding AddTaskCommand}" />

        <CollectionView ItemsSource="{Binding Tasks}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Spacing="10" Padding="5">
                        <CheckBox IsChecked="{Binding IsCompleted}" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainViewModel}}, Path=ToggleTaskCommand}" CommandParameter="{Binding .}" />
                        <Label Text="{Binding Title}" VerticalOptions="Center" />
                        <Button Text="Удалить"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainViewModel}}, Path=DeleteTaskCommand}"
                                CommandParameter="{Binding .}" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>